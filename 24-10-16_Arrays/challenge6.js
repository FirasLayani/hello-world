// Create a function called reverseArrayElements that takes an array of strings as an argument. 
// The function should reverse the order of the array and return a new array

// (Array methods slice() and reverse() can make this a shorter, more concise function.)

function reverseArrayElements(array) {
    return array.reverse()
}

let foods = ['apple', 'banana', 'Mango', 'POTATO']

console.log(reverseArrayElements(foods))